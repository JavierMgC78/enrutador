# /.htaccess (raíz)
RewriteEngine On

# Deja pasar archivos y directorios reales
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Reescribe todo hacia public/index.php SIN cambiar la URL
RewriteRule ^ public/index.php [QSA,L]